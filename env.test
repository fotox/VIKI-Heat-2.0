# Test Environment
FLASK_ENV=testing
FLASK_DEBUG=0
LOG_LEVEL=INFO
BUILD_TARGET=test

# Test Database
POSTGRES_DB=viki_test
POSTGRES_USER=viki_test
POSTGRES_PASSWORD=test-secret
POSTGRES_PORT=5433

# Test Redis
REDIS_PORT=6380
REDIS_PASSWORD=

# Security (Test Keys)
SECRET_KEY=test-secret
JWT_SECRET_KEY=test-jwt-secret
MASTER_RESET_KEY=test-master-key

# Backend
BACKEND_PORT=5001

# CORS
CORS_ORIGINS=*

# Scheduler (deaktiviert für Tests)
SCHEDULER_INTERVAL=0
